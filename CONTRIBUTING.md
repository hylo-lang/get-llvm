# Contributing

## Requirements

The project requires Node 24.11. You can also use the provided devcontainer configuration to edit the project in Docker.

## Publishing a release

Just push a git tag of the form `v0.0.0`.

Before pushing, run `npm run pack`.